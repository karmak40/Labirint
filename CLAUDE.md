# Labirint

Godot 4.7, GDScript, 2D top-down, all graphics procedural (`_draw()`), no sprites.

## Current direction: RTS "база на базу"

The full approved plan lives in [docs/rts_plan.md](docs/rts_plan.md). Read it before starting any RTS work. It has the architecture, milestones M0–M9 and how to verify each one.

Progress: plan approved on 2026-09-24. M0 to M8 done on 2026-09-24; M9 partly done (batched rendering, tower weakened), and optimisation is on hold until the game is tuned.
- M0/M1: `Team`, `PlayerBody.team`/`team_of()`, `Knight`→`Unit` (`scripts/unit.gd`), `Order`/`set_rally`/`set_attack_move`, `scripts/squad.gd`. Sides come from the scenes: `Player.tscn` has `team = 1`, `Knight.tscn` has `team = 2`.
- Friendly fire is off (decided with the user): `PlayerBody._nearest_target` and `hit_target_at` skip `Team.allied` bodies. NEUTRAL is nobody's ally, so the dummy still gets hit.
- M2: `NavFloor` (`scripts/nav_floor.gd`) bakes the floor from static colliders. `Pathfinder` (`scripts/pathfinder.gd`, a `NavigationAgent2D` with RVO) is shared by `Unit` and `Worker` and created in code. Without a nav region a `Unit` walks along X only, which keeps the testbed unchanged. Idle bodies give way to the crowd (`settle(wish, give_way)`).
- M3: `Economy`, `PlayerState` (economy + squad; `_claim_field` hands its economy to stockpiles of its team), `Stockpile` (banks resting `Beam`/`Rock` within `RADIUS`), `Worker` + `scenes/worker/Worker.tscn` (`job` = wood/ore/gold), `OreVein.resource_kind` with `Rock.resource_kind` for gold. Resources are banked only on delivery. Test scenes: `scenes/tests/EconomyTest.tscn`, `scenes/tests/BattleTest.tscn`.
- M4: `Building` (`scripts/building.gd`, `StaticBody2D` with the dummy's target contract, footprint collider built in code, health bar) with subclasses `Base` (`base_building.gd`, `base_destroyed(team)`), `ProductionBuilding` (`CATALOG` of kinds/costs/times, `queue_unit(kind)`, `set_rally_point`, `unit_ready`) and `Tower` (its own homing bolts, shoots bodies only). `PlayerState` claims buildings (`building.side`), and `base()`/`barracks()` claim lazily. `Unit` scans every 0.25 s with a random phase, prefers people over walls (`WALL_BIAS`), and treats a blocked attack-move goal as reached within `GOAL_SLACK`. Test scene: `scenes/tests/BaseTest.tscn`.
- M5: `MapData` (`scripts/map_data.gd`: floor, trees, veins, gold, `player`/`enemy` as `SideLayout` sub-resources with base, barracks, stockpile, towers, rally, start_workers and start_stock), `MapBuilder` (`scripts/map_builder.gd`, builds everything in `_ready`, `states[team]`/`state_of(team)`), `resources/maps/skirmish_small.tres` (900×560, mirrored left to right, fits the screen without a camera), `scenes/main/Skirmish.tscn`. Trees are 190 px tall, so keep them in the bottom strip or they cover whatever stands above them.
- M6: `GameState` autoload (`scripts/game_state.gd`: map, sides, phase MENU/PLAYING/WON/LOST, `match_ended`, and the commands `hire`/`attack_move`/`attack_enemy_base`/`rally_home`/`set_rally_point`, each taking a team; the AI in M7 must use the same ones). `scenes/ui/MainMenu.tscn` is now `run/main_scene` (decided at M6; its "Тестовый стенд" button opens `Main.tscn`). `scenes/ui/Hud.tscn` (`scripts/ui/hud.gd` builds its UI in code; `player_input_adapter.gd` maps RMB to an attack-move and Shift+RMB to the rally point), `scripts/ui/rts_camera.gd`. Gameplay ticks (`ProductionBuilding`, `Tower`, `Stockpile`) run in `_physics_process`. `Unit.leash` (200 px from its post, set only by orders). `Pathfinder._unstick` makes a stalled walker sidestep; workers spread across different sources. `Unit`/`Worker` turn off `_unhandled_key_input` (the body jumps on Space). `MapBuilder` finds `GameState` through `/root/GameState` rather than by name, so a map still builds without the autoload.
- M7: `AIDirector` (`scripts/ai_director.gd`), a child of its side's `PlayerState`, added by `GameState.register_match` for every side the human is not playing. It thinks every ~2 s with jitter (the only gameplay randomness), acts only through the `GameState` commands, and sees the enemy only through the `targets` group. Its plan: 2+2 workers first, then whichever trade a knight is short of, then waves (4, +2 each, up to 12) with retreat when spent, home defence, and a counter-attack after repelling one. The menu has an "ИИ против ИИ" mode (`GameState.spectating`; the HUD then shows both stores and hides the command bar). `ChopTree.regrow_time` and `OreVein.refill_time` (0 = never, which keeps the testbed unchanged) come from `MapData`; skirmish_small uses 40 s and 25 s. The woodcutter costs ore and the miner costs wood, so a side can't get stuck with an empty store. `Unit.HAND_BIAS`: on attack-move, units chase workers only when very close. Workers skip sources inside hostile tower range. Units and workers draw a team-coloured ring at their feet (`Team.draw_foot_ring`). `AIDirector` also looks up GameState through `/root/GameState`, because GameState creates it.
- AI vs AI check (headless, `--fixed-fps 60`): 7 of 8 matches finished on their own, between 2.5 and 11 minutes; both colours win.
- M8: `Achievements` autoload (`scripts/achievements.gd`, a `ConfigFile` at `user://achievements.cfg`, 10 achievements in `LIST`, `unlock(id)` is idempotent, `track(event, amount)` keeps running totals). It listens only to signals, starting at `match_started`: the new `PlayerBody.died` (one line in `_start_death`), `Tower.destroyed`, the human side's `Stockpile.received` and `ProductionBuilding.unit_ready`, and `match_ended`. Nothing counts in AI vs AI. `GameState.match_time` counts play time. The HUD shows an unlock toast, and the menu has an "Достижения" screen. **Any test that plays a match must set `LABIRINT_ACHIEVEMENTS=<path>`, or it writes into the real save.**
- M9 in progress. Target from the user: phones with 40+ units per side. Measurements on this PC (GL Compatibility, real frame with rendering; N knights per side fighting on skirmish_small):
  - Start: N=20 21.2 ms, N=40 39.3 ms, N=60 56.7 ms. The bottleneck was about 33 draw calls per rig figure. Simulation (targets, nav, AI) at N=60 is about 4.6 ms, so `SpatialGrid` is not needed.
  - Done: `Pen` (`scripts/pen.gd`) gathers a figure into one triangle array (1 draw call). `walker.gd` routes all its `draw_*` calls through `_w_*` wrappers and draws through the Pen when `batched` is set. `batched` defaults to `Pen.crowd_mode`, which `MapBuilder` raises in `_enter_tree` and lowers in `_exit_tree`, so the testbed draws exactly as before (4.2 ms, unchanged). In crowd mode a rig animates every other frame (`CROWD_STRIDE`, round-robin `crowd_slot`), and its hit/grab events fire on crossing a threshold, so nothing is lost. `Pen.feather` (1 px soft edge) replaces antialiasing, because GL Compatibility has no 2D MSAA (MSAA is only enabled on other renderers). Team rings are drawn only in crowd mode.
  - Now: N=20 11.4 ms, N=40 19.3 ms, N=60 27.7 ms (without feather: 8.1 / 13.5 / 19.0). What remains is CPU in GDScript: Pen tessellation (~11 ms without feather, ~19 ms with it, at 132 figures) and rig animation (~4.5 ms).
  - Tower weakened (M9, the user's decision): range 150, damage 9, every 1.4 s. The user plans that towers will later be built for resources and won't be on the map at the start. Gold is for later (the idea is weapon research and unlocking units).
  - On hold, the user's decision on 2026-09-24: game design and tuning first, further optimisation later. Options for then: quality settings in GDScript (feather off, stride 3, simplified small figures) or moving the Pen and rig maths to C#/GDExtension.
- Long map (user request, 2026-09-24): `resources/maps/skirmish_long.tres` (3000×560, mirrored left to right) is now `GameState.DEFAULT_MAP` and is used by `Skirmish.tscn`. It starts with no towers and no workers, and 60 wood + 60 ore. Veins hold 20 rocks × 10 = 200 ore (gold: 10 × 10), a tree drops 10 logs × 5 wood, trees regrow after 90 s, and veins don't refill. These come from `MapData` (`ore_rocks`/`ore_rock_worth`/`gold_*`/`tree_logs`/`log_worth`) and are passed to `OreVein.rocks`/`rock_worth`, `Rock.amount`, `ChopTree.logs`/`log_worth` and `Beam.amount`, which `Stockpile` banks; the defaults are the testbed's old values. Prices ×10: woodcutter 10 ore, miner 10 wood, gold miner 15 wood, knight 20 wood + 20 ore. The `Base` is a big castle (footprint 300×110, drawn about 290 px tall, 2000 HP). Big targets are struck at their nearest point: `Team.spot()` / `Building.nearest_point()` in `PlayerBody._nearest_target`/`hit_target_at` and in `Unit`, and `attack_enemy_base` goes to `Building.approach_from()`. The camera opens on your own castle. The achievement totals were scaled ×10 too (`wood_1000`, `ore_500`, `gold_100`). "Ломать — не строить" is unreachable until towers can be built. `skirmish_small.tres` remains as the old map.
- Warrior, research, stamina fix (user request, 2026-09-24):
  - `Weapon.CLUB` (end of the enum; `STRIKE_COST` 14, `STRIKE_HARM` 16; drawn by `walker._stroke_club`; every other behaviour falls through to the axe's default branch).
  - `Unit.loadout` is "knight" or "warrior" (`_kit_out`). `scenes/warrior/Warrior.tscn` is a warrior with a club, no armour, 100 HP.
  - `ProductionBuilding.CATALOG`: "warrior" costs 15 wood, 5 s (wood only, because ore runs out on the map and wood regrows). "knight" has `requires: "chivalry"`. `is_unlocked()` checks it.
  - Research lives in `PlayerState` (`RESEARCH`, `researched`, `researching`/`research_left`, `start_research`, `research_done`); the command is `GameState.research(team, id)`. "Рыцарство" costs 60 ore + 40 gold and takes 30 s, which is gold's first use. The HUD has a research button, and a locked unit shows "нужно: …".
  - AI: researches chivalry, hires 1 gold miner after the first wave, saves ore once it has the gold (`_saving_for_chivalry`), and fields the best soldier it knows (`_soldier_kind`).
  - `Worker.SEARCH` is 1600 (the long map).
  - Stamina fix: an out-of-breath unit now stands facing its enemy and waits (it used to back away and turn round); stepping back when crowded is a backpedal (`facing_locked`, reset each `_decide`). Checked on a castle: 0% of the time turned away, versus 17% before.
  - The leash is measured to `Team.spot` (it was measured to the castle centre, so a unit at the wall sometimes ignored the castle).
  - The long map has 45 trees in a two–three row band along the bottom, and the veins are raised above the tree line.
- Landscape (user request, 2026-09-25; room 1 of the campaign is the current long map): `Terrain` (`scripts/terrain.gd`) replaces the grey floor, grid and border in `MapBuilder._lay_floor`. It adds grass with patches, tufts, flowers and pebbles, a road between the castle gates with side paths to the barracks and stockpile, worn earth, gravel round the veins, forest floor under the trees, and beyond the far edge a sky gradient, mountains, hills, a tree line and drifting clouds. The near edge gets an earth bank. It is presentation only, seeded from `map.title`, and drawn once through `Pen` into 500 px strips in two layers (`beds` first, then details, so nothing overhanging a strip edge gets painted over). `Pen` gained `gradient_quad` and `ellipse`. The camera can scroll up `SKY_VIEW` = 180 px into the sky. Cost: empty match 4.8 → 6.0 ms, 40 per side 19.3 → 20.8 ms. The testbed keeps its old floor.
- Campaign (built 2026-09-25; the user chose separate levels and the gorge + siege rooms):
  - `Campaign` autoload (`scripts/campaign.gd`): `ROOMS` (forest / gorge / siege, each with a map, briefing, goal, `fast` seconds and `castle` share for the stars, plus `waves` for the siege). Progress lives in `user://campaign.cfg` (override with `LABIRINT_CAMPAIGN`; tests use their own file). Room 1 is always open; each later one opens once the previous is won. `start(i)`, `next_room()`, `leave()`, `last_earned`. It finds GameState lazily, because autoloads come up in order.
  - Stars: 1 for a win, +1 within `fast`, +1 with the castle at or above `castle`.
  - `RoomRules` (`scripts/room_rules.gd`) is added to the field on `match_started` while a room is being played. For the siege it keeps `Base.shut` (the gates are barred and take no damage), sends the waves at their times from the enemy gate as an attack-move on our castle, and opens the gates once the last wave is beaten (`gates_opened`). `objective()` feeds the HUD's objective line.
  - Maps: `campaign_forest.tres` (the long map), `campaign_gorge.tres` (48 `Boulder`s make the gorge walls; the only gold is inside it; 2 enemy towers at the exit), `campaign_siege.tres` (we start with 160 wood + 120 ore and a tower; the enemy has 3 towers and 3 workers). `Boulder` (`scripts/boulder.gd`) is a static rock from `MapData.boulder_positions`, and nothing can be built on it.
  - UI: a "Кампания" button in the menu opens the list of rooms with stars and locks, then a briefing with "В бой". The HUD shows the objective line and messages for waves and the gates opening. On a win the end screen shows ★ and "Следующая комната". The menu reopens the room list after a room. "Схватка" and "ИИ против ИИ" call `Campaign.leave()` first.
  - Checked by `tests/test_campaign.gd`.
- UI, construction, library (user request, 2026-09-25):
  - `Building` can be put up in a match: `start_construction(seconds)`, `built` 0..1, health grows from 15%, `completed` signal, a scaffold site drawing. `Tower`/`ProductionBuilding` call `_tick_construction` first.
  - `GameState.BUILDINGS` (tower 40 wood + 30 ore, 20 s; library 60 wood + 20 ore, 25 s, one per side), `build_problem(team, kind, point)` (returns the reason or ""), `build()`, and the `built` signal. You may build within 700 px of your own buildings. `NavFloor.rebake(true)` rebakes in the background.
  - `Library` (`scripts/library.gd`). Research happens only in a finished library and is lost if the library falls (`research_lost`). `PlayerState.RESEARCH`: chivalry (knights), forging (+25% damage), mail (+25% health). Upgrades go through `PlayerState.kit_out` → `Unit.set_scales`, for soldiers already in the ranks too.
  - AI: builds a library once it has 3 workers, saving wood for it (`_saving_for_library`). While it saves for the library or a study, it still hires workers for whatever it is short of (`_hands_for`). Study order: forging (needs no gold), chivalry, mail. A gold miner is hired once the library stands. Up to 2 towers in front of the castle. Checked by `tests/test_ai_builds.gd`.
  - HUD rewritten: `scripts/ui/ui_style.gd` (dark wood and gold theme), `icons.gd` (drawn icons), `hud_card.gd` (cards), `minimap.gd` (click to move the camera), tabs Найм / Постройки / Знания, a status column, order buttons, messages. The adapter has a placement mode (green or red silhouette with the reason; LMB places, Shift keeps placing, RMB/Esc cancels). Hotkeys: 1–5 cards, Tab changes tab. The menu (drawn evening backdrop with a castle) and the achievements screen use the same `UiStyle`.
- More troops (user request, 2026-09-25):
  - `Unit.LOADOUTS` is the table of kinds: knight, warrior, spearman (spear, helm, 1.15 health), archer (bow, 0.9, `reach` 220) and crossbowman (crossbow, helm, `reach` 250). `_kit_out` reads it. A kind with `reach` is a shooter: `_shoot()` stands off at its reach, backpedals (facing locked) from anyone within 90 px, and loosens at `aim_point` with a little lead. A crossbow's `attack()` spans it when empty.
  - Aiming: `PlayerBody.aim_point` (INF by default, as for the testbed player) makes `walker._release_arrow` call `_aim_arrow`, which sends the shaft across the field at that point, lofted to land there. Without it the old straight-ahead shot is unchanged.
  - Hiring: spearman (15 wood + 5 ore, needs «Копья»), archer (20 wood, «Луки»), crossbowman (15 wood + 15 ore, «Арбалеты»). All three use `Warrior.tscn` with a `loadout` from `CATALOG`.
  - Research gained spears, archery and crossbows. `needs` is a prerequisite, and crossbows needs archery (`PlayerState.prerequisite_met`).
  - HUD: tabs Рабочие / Войска / Постройки / Знания. Locked research shows what it needs.
  - AI: study order forging, spears, archery, chivalry, mail, crossbows. It fields a mixed army (`MIX`: knight 3, spearman 2, archer 2, crossbowman 1.5) of whatever is unlocked, and warriors only while nothing else is open.
  - All rig weapons are hireable now (user request, 2026-09-25), each behind its own study: axeman «Секироносец» (AXE, «Секиры», `siege` 1.5), swordsman «Мечник» (SWORD, «Клинки»), greatsword «Двуручник» (GREATSWORD + armour, «Двуручные мечи», needs blades + gold), scout «Лазутчик» (DAGGER, «Кинжалы», `speed` 1.3, `hunts`: goes for workers first), torchbearer «Поджигатель» (TORCH, «Поджог», `siege` 4.0; `Unit.torch_strike` is a no-op so it never burns the forest), mage «Маг» (STAFF, «Магия», gold, `reach` 260). The pickaxe stays a miner's tool. `PlayerBody.harm_against(mark)` (plain `strike_harm()` in the testbed) is where `siege` applies. Staff bolts aim at `aim_point` like arrows, and arrows, quarrels and bolts now carry `strike_harm()`, so forging helps shooters too. HUD tabs are data (`TABS`: Рабочие / Пехота / Особые / Постройки / Оружие / Науки, up to 6 cards each). AI mixes and study lists include the new kinds. Checked by `tests/test_new_troops.gd`.
  - Healing (user request, 2026-09-25): study «Исцеление» (40 ore + 40 gold, 25 s, needs magic; HUD tab «Магия» next to «Науки», tab buttons 78 px). `PlayerState.kit_out` calls `Unit.set_mending`, and only a kind with `mends` (the mage) can heal. `Unit._mend` runs before the watch each `_decide`. It heals the most hurt ally within `MEND_REACH` 200 (itself included): below `MEND_WOUNDED` 0.85 when not fighting, below `MEND_URGENT` 0.45 during a fight, never with an enemy within `SHOOTER_CLOSE`. A heal is +30 HP, cast with the staff swing, at most every 1.6 s. The rig's `_release_bolt` asks `player.release_mend()` first (duck-typed, so the testbed is unchanged), and a `MendGlow` (`scripts/mend_glow.gd`) shows it. The balanced, turtle and tech AIs study it after magic. The forge is described in the next item. Known flake: `test_castle_assault` fails about 1 in 10 runs ("4 frames turned away"), on the committed code too.
  - Forge «Кузница» (user request, 2026-09-25; the user chose helm + plate + shield, bought one piece at a time): `Forge` (`scripts/forge.gd`, a `Building`, `GEAR` helm 15 ore 6 s / armour 30 ore + 10 wood 10 s / shield 20 wood + 5 ore 6 s, queue of up to 5, `queue_gear`, `forged`). It is in `GameState.BUILDINGS` (50 wood + 40 ore, 25 s, one per side), and the command is `GameState.forge(team, item)`. A finished piece goes to `PlayerState.receive_gear`: it is put straight on the nearest soldier who can use it (`Unit.can_wear`/`wear`), or it waits in `PlayerState.gear` for the next recruit (`kit_out` → `_fit_gear`). Kit now protects (in `Unit.take_hit` only, via `guard_against`): helm ×0.9, plate ×0.8, a shield ×0.7 against blows and shots from in front. So existing helmed and armoured kinds, knights included, got tougher. A shield fits only one-handed weapons (`PlayerBody.SHIELD_WEAPONS`: sword, club, axe, dagger, torch; `PlayerBody.shielded`, and `walker._has_shield` draws it and puts the off arm on it). HUD: a «Кузница» card in «Постройки» and a «Кузница» tab (helm/armour/shield with the queue; the tooltip shows the store and how many lack one). Tab buttons are 68 px at font 11, 8 tabs. AI: `plan["gear"]` (the order it likes), it saves for a forge after its first study (`_wants_forge`/`_saving_for_forge`), towers come before kit (`_wants_towers`), and it orders only what somebody can wear. Checked by `tests/test_forge.gd` and `test_ai_builds` (which now switches the AI's attacks off: against a passive side it wins by about 100 s).
  - Smithing (user request, 2026-09-25): the forge makes nothing by itself. `Forge.GEAR` gives the `work` in seconds of hammering, and each blow adds `BLOW_WORK` 0.75 through `hammer_blow()`. There is an anvil in front (`ANVIL`, drawn ×`ANVIL_SIZE`, with a hot piece and sparks), and the smith stands at `SMITH_SPOT`; the progress bar is under the anvil. A smith is a `Worker` with `job = "smith"`, holding the new `Weapon.HAMMER` (end of the enum; `walker._stroke_hammer`; axe pose). `set_job()` remembers `former_job`. `_smith()` walks to the spot and swings `attack()` while there is work, and `Worker.land_strike` at the anvil calls `hammer_blow()` instead of hitting anyone. `PlayerState.smith()/assign_smith()/release_smith()`: it takes a worker from whichever of wood and ore has the most hands (gold last), the one nearest the forge; one smith per side. The commands are `GameState.assign_smith/release_smith(team)`. HUD: the «Кузнец» card (1) in the «Кузница» tab assigns a smith, and pressing it again sends him back; its badge says назначить / идёт / ждёт / куёт. Ordering with no smith says the order waits. The AI assigns a smith as soon as its forge stands. Pace: 28 s of work took about 29 s in `test_forge`.
  - Workers and barracks (user request, 2026-09-25): labourers are all alike. `CATALOG["worker"]` has `job` "auto": it goes to whichever of wood and ore has fewer hands (`PlayerState.least_staffed_job`). `woodcutter`/`miner`/`gold_miner` are the same hire sent to one trade (the AI uses them). All of them cost `pay_any` 10, taken from whichever of wood and ore the store has more of (`ProductionBuilding.price_for`). `PlayerState.hands()`/`move_worker(job)` and `GameState.assign_worker(team, job)` move one hand from the trade with the most. `Worker.set_job` changes tools. `Base` now extends `ProductionBuilding` and hires the `LABOURERS` at the castle gate (`_door()`; `redraw_while_hiring` is off, and its colour constants were renamed `KEEP_ROOF`/`GATE_DOOR`). A barracks hires `SOLDIERS` only and is a building in `GameState.BUILDINGS` (40 wood + 10 ore, 20 s, one per side). `SideLayout.has_barracks` is false on skirmish_long and the three campaign maps, and `barracks` there is just the cleared spot. `ProductionBuilding.can_hire` needs `is_complete()`. `PlayerState.barracks()` skips the castle, and `producer_for(kind)` returns the castle for labourers and the barracks for soldiers. The rally point lives in `PlayerState.rally_point`, and a new barracks takes it in `adopt`. HUD: «Рабочие» holds Рабочий + Лес / Руда / Золото (a click moves one hand; the badge shows the count), «Постройки» starts with «Казарма», and the status row is «Найм» (castle and barracks queues). AI: builds the barracks after its first hands (`_first_hands`), hires only hands until then, `_rebalance` moves a hand when one store leads by `GLUT` 80, and `library_after_wave` (rush) holds the library until its first wave is out; a rush now attacks at about 57 s. Achievements hear barracks built mid-match (`built`). Tests: `raise_barracks(team)` in `test_case.gd` stands one up instantly for tests that hire soldiers; `tests/test_workers.gd` covers all of this.
  - Workers are on their own physics layer (`Worker.WORKER_LAYER` 2, mask 1): they collide with the world but not with bodies. Five woodcutters shoving each other between the trunks used to wedge for good. `Pathfinder._unstick` now measures stalling by how far the body actually moved (not its reported velocity), and alternates the sidestep side. Before this, the balanced AI's wood stayed at 0 for 2 minutes.
- AI difficulty and strategies (user request, 2026-09-25):
  - `AIProfile` (`scripts/ai_profile.gd`): `STRATEGIES` are balanced «Ровный», rush «Натиск» (spearmen + warriors, only spears/forging, no towers, waves from 3, fights to the last), turtle «Оборона» (4 towers early, archers/crossbowmen, first attack not before 480 s, wide home guard), and tech «Знания» (big economy, knights + crossbowmen, attacks only after chivalry + forging). `make(strategy, difficulty)` merges a strategy with a difficulty. Easy: slower thinking, 1 hire per thought, fewer workers, smaller waves, no counter-attack, only 3 studies, one tower fewer, +180 s before attacking. Hard: faster thinking, more hires and workers, bigger waves, pulls back sooner, attacks earlier. No difficulty cheats. "random" picks a strategy.
  - `AIDirector` reads everything from `plan` (the former constants), gates attacks with `_may_attack` (`attack_after`, `attack_needs`), and uses `_saving_for_study` for any study.
  - `GameState.ai_strategy` (default "random") and `ai_difficulty` (default "normal") go to the enemy's director; in AI vs AI both sides are random. Campaign rooms set the strategy (`ai`: forest balanced, gorge turtle, siege tech); difficulty is the player's choice.
  - UI: «Схватка» opens a setup panel (difficulty + enemy strategy) with «Начать». The briefing shows the room's enemy strategy and a difficulty choice. The end screen names the enemy's strategy and difficulty, and AI vs AI shows each side's strategy.
  - Checked by `tests/test_ai_strategies.gd`: profiles, a rush attacked at about 57 s (28 s before barracks had to be built) and won against a passive side by about 120 s, a turtle had 4 towers by 240 s and never went out. Tests that depend on a particular AI set `ai_strategy` themselves.
- People + arms (user request, 2026-09-26; plan in [docs/weapons_plan.md](docs/weapons_plan.md)): a soldier is a man with arms. `GameState.hire(team, soldier)` now calls `PlayerState.order_soldier`: it pays for the man (`CATALOG["recruit"]`, `pay_any` 10) plus whatever of his `arms` is not free in store, queues those pieces at the least busy forge (`Forge.queue_paid`) and the man at the castle (`queue_paid("recruit")`), and adds a `Draft` (`scripts/draft.gd`: HIRING → ARMING → CARRYING). The castle's recruit is a `Worker` with `job = "recruit"` (not counted in `workers()`); `take_recruit` gives him the oldest draft. He walks to the nearest forge's `collect_point` (the castle gate if there is none), takes his pieces there (`hand_out_arms`), carries the weapon in his hands, walks to `barracks.door_point()` and `send_to_train` turns him into a `train(kind)` entry (`TRAIN_TIME` 3 s). A warrior's club is free (`arms: ["club"]`), so warriors need only a barracks. If the recruit dies, the draft is lost. `_supply` re-orders pieces lost with a forge. At most `DRAFT_MAX` 10 drafts.
  - `Forge.GEAR` now includes the weapons (spear, bow, crossbow, axe, sword, greatsword, dagger, torch, staff, with prices from the plan) and `KIT` (helm/armour/shield). Studies unlock recipes: `PlayerState.knows_recipe(item)` (kit is always known) and `kind_unlocked(kind)`. Store: `gear` (all items), `reserved(item)` (put by for drafts), `spare(item)`. Kit auto-fits only from spare. A knight needs sword + helm + armour + shield (about 110 in total).
  - A forge has two anvils (`ANVILS`, `on_anvil`, `worked`, `hammer_blow(anvil)`, `pending(item)`, `all_pending()`). `queue` only holds what is waiting for an anvil. Two smiths per forge (`Worker.smithy`/`anvil`; `PlayerState.smiths()`, `free_anvil()`), and up to two forges per side (`BUILDINGS["forge"]["limit"]` 2). A smith whose forge falls goes back to his old job.
  - `ProductionBuilding.queue_unit` still hires outright at catalogue prices (BaseTest uses it). HUD: soldier cards show `draft_price`, the stage of the order («ждёт: копьё», «несёт», «обучается»), and the reason on failure. Shift+click makes the arms into store (`_stock`). The «Кузница» tab has a «Склад» card, and the smith card shows smiths/anvils (it assigns while an anvil is free, otherwise releases one). Research texts say «Кузница учится ковать оружие для …».
  - AI: builds a forge after its first study whatever its `gear`, fills free anvils with smiths while it has more than `_first_hands()` labourers, counts drafts and training as coming (`_coming`), picks only kinds it can arm (warriors without a forge), and makes up to `STOCK` 2 weapons ahead when the forge is idle and it can afford two soldiers. `test_ai_builds` now waits for an armed soldier (a spearman at about 313 s).
  - Tests: `raise_forge(team)` in `test_case.gd`; `tests/test_arms.gd` covers the chain (a spearman takes about 11 s from the order to the ranks, a knight with two smiths about 21 s). Balance for the new prices is still to do.
- Tests (2026-09-25): `tests/` has 21 fast tests (plus `test_arms`, 2026-09-26) (including `test_troops`, `test_new_troops`, `test_forge`, `test_workers`, `test_campaign` and `test_ai_strategies`), including `test_hud_input` (real keys and clicks via `push_input(event, true)`, so it works headless) and `test_ai_builds` (one AI against a passive side) (`test_*.gd`, base `tests/lib/test_case.gd`: `begin()`/`step()`/`check()`, exit code 0/1, its own achievements file). Run all with `tests/run_tests.ps1` (about 40 s) or `-Only <name>`. `tests/bench/` holds window-only measurements. No AI-vs-AI match tests (removed at the user's request): check the AI with short targeted tests such as `test_ai_builds`. Known issue: a test process sometimes segfaults on exit after passing (the engine's shutdown in `-s` mode; never seen in the game). The runner trusts the printed `PASS:` line. `test_economy` very rarely leaves one ore rock unmined (1 in about 19); it prints diagnostics when that happens.
Notes for M9: rare stalemates remain, and the defender's tower plus home recruits is strong. The mirror 3v3 in BattleTest can go either way.

Key decisions made with the user:
- The map stays top-down with free movement, not a side-view lane, so real pathfinding is needed (NavigationAgent2D).
- Multiplayer is architecture-only for now: no `Input.*` below the command layer, commands take primitive args or ids, each side owns its own state. No netcode yet.
- Economy: resources count only when a worker physically brings a `Beam`/`Rock` to the stockpile, never at the moment of the strike. The same rule applies to the AI.

## Do not break

`scenes/main/Main.tscn` and `scripts/debug_panel.gd` are the animation and combat testbed. Build the RTS mode in new files and scenes (`scenes/main/Skirmish.tscn` etc.), and keep `PlayerBody`, `walker.gd` and `Gait` working for the testbed at every milestone.
